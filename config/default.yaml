# デフォルト設定
#
# ガイド: アプリケーションのデフォルト設定値をここに記述してください。
#   環境ごとの上書きは環境変数または環境別設定ファイルで行います。
#   詳細は docs/dev/config-reference.md を参照してください。

app:
  name: "{ツール名}"
  port: 3000
  log_level: "info"    # debug | info | warn | error

# database:
#   host: "localhost"
#   port: 5432
#   name: "{DB名}"
#   pool_size: 10
