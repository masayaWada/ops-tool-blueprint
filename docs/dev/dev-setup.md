# 開発環境セットアップ

<!-- ガイド: リポジトリをクローンしてからローカルで動作確認できるまでの手順を記載してください。前提条件を冒頭に明記し、手順通りに進めれば必ず環境が立ち上がる状態を目指してください。 -->

## 前提条件

<!-- ガイド: 必要なツール・バージョン・アクセス権限をすべて冒頭に列挙してください。ここを読んだ時点で不足物が分かるようにします。 -->

| ツール / 権限 | バージョン / 条件 | 確認コマンド |
|---|---|---|
| {ツール: 例 Node.js} | {バージョン: 例 18 LTS 以上} | `node --version` |
| {ツール: 例 Docker} | {バージョン: 例 20.10 以上} | `docker --version` |
| {ツール: 例 AWS CLI} | {バージョン: 例 v2} | `aws --version` |
| {権限: 例 AWS dev アカウントへのアクセス} | {条件: 例 SSO ログイン済み} | `aws sts get-caller-identity` |

## セットアップ手順

### 1. リポジトリをクローン

```bash
git clone {リポジトリURL: 例 https://github.com/{org}/{repo}.git}
cd {repo}
```

### 2. 依存関係のインストール

```bash
{インストールコマンド: 例 npm install}
```

### 3. 環境変数の設定

```bash
cp .env.example .env
```

<!-- ガイド: .env に設定が必要な値と、その取得方法（例: Secrets Manager から取得、Slack で依頼 等）を記載してください。 -->

`.env` に以下の値を設定してください:

| 変数名 | 取得方法 |
|---|---|
| `{変数名: 例 DATABASE_URL}` | {取得方法: 例 ローカル Docker の接続文字列を使用} |
| `{変数名: 例 API_KEY}` | {取得方法: 例 開発用の値を Secrets Manager から取得} |

### 4. ローカルサービスの起動

<!-- ガイド: DB やキャッシュなど、アプリケーション以外に必要なサービスがある場合に記載してください。 -->

```bash
{起動コマンド: 例 docker compose up -d}
```

### 5. アプリケーションの起動

```bash
{起動コマンド: 例 npm run dev}
```

### 6. 動作確認

```bash
# ヘルスチェック
{確認コマンド: 例 curl http://localhost:3000/health}

# テストの実行
{テストコマンド: 例 npm test}
```

## よく使う開発コマンド

| コマンド | 説明 |
|---|---|
| `{コマンド: 例 npm run dev}` | {説明: 例 開発サーバーを起動（ホットリロード有効）} |
| `{コマンド: 例 npm test}` | {説明: 例 テストを実行} |
| `{コマンド: 例 npm run lint}` | {説明: 例 リンターを実行} |
| `{コマンド: 例 npm run build}` | {説明: 例 プロダクションビルド} |
| `{コマンド: 例 npm run format}` | {説明: 例 コードフォーマット} |
